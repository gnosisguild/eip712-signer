describe("EIP7127Encoder - Structs", () => {
  describe("Atomic Types", () => {
    it.skip("should hash a struct with atomic types (uint256, bool, address)", () => {
      // Test hashing a simple struct with atomic types.
    });

    it.skip("should handle edge cases for integer values (min/max values)", () => {
      // Test hashing integer types with their minimum and maximum values.
    });

    it.skip("should handle boolean values (true/false encoding)", () => {
      // Test hashing boolean values.
    });

    it.skip("should handle address type encoding", () => {
      // Test hashing address types, including edge cases like the zero address.
    });

    it.skip("should handle different length byte types (bytes1 through bytes32)", () => {
      // Test hashing fixed-length byte types.
    });
  });
});
