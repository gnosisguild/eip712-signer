describe("EIP7127Encoder - Structs", () => {
  describe("Nested Structs", () => {
    it.skip("should hash a struct with nested structs (multiple levels deep)", () => {
      // Test hashing a struct that contains nested structs.
    });

    it.skip("should handle deeply nested structs", () => {
      // Test hashing deeply nested structs to ensure the encoder can handle recursion limits.
    });
  });
});
