describe("EIP7127Encoder", () => {
  describe("hashTypedData()", () => {
    it.skip("should hash typed data with a valid struct and domain separator", () => {
      // Test the main functionality of hashing typed data.
    });

    it.skip("should handle multiple primary types in the same domain", () => {
      // Test hashing multiple primary types within the same domain.
    });

    it.skip("should handle mixed static and dynamic types", () => {
      // Test hashing a struct that contains a mix of static and dynamic types.
    });
  });
});
