describe("EIP7127Encoder - Rejection", () => {
  describe("Recursive Structs", () => {
    it.skip("should reject recursive struct references", () => {
      // Test that the encoder rejects recursive struct references (if not supported).
    });
  });
});
