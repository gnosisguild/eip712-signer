describe("EIP7127Encoder - Rejection", () => {
  describe("Invalid Types", () => {
    it.skip("should reject invalid struct definitions", () => {
      // Test that the encoder rejects invalid struct definitions.
    });

    it.skip("should reject type mismatch errors", () => {
      // Test that the encoder correctly handles type mismatches.
    });

    it.skip("should reject unsupported types (e.g., mapping, function)", () => {
      // Test that the encoder rejects unsupported types.
    });
  });
});
